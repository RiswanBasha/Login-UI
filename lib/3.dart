import 'package:flutter/material.dart';

class ThirdOption extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text(
        'I Like App Developing',
        style: TextStyle(fontSize: 27.0,color: Colors.white),
      ),
    );
  }
}